# 2026-03-27_001 — End-to-End Test Fixes

## What changed
- **Fixed follow-up endpoint** — `lead/[id]/page.tsx` changed PATCH call from `/api/leads/${id}` to `/api/leads/${id}/followup` (matching the actual route)
- **Fixed share panel not opening** — `demo/[code]/page.tsx` added `await` to `generateShareLink()` so `setShowSharePanel(true)` runs after the link is ready
- **Fixed Stripe checkout auth error** — added `/api/payments/create-checkout` and `/api/payments/webhook` to `PUBLIC_PATHS` in middleware (client paying isn't logged in)
- **Fixed Stripe SDK on Vercel** — replaced Stripe Node SDK with raw `fetch` to Stripe API in `create-checkout/route.ts` to fix SDK connection errors on Vercel's serverless runtime
- **Admin assign now works** — confirmed admin API requires `admin_token` cookie from password auth

## Why
Comprehensive end-to-end testing before sending salespeople out with demo sites. Every step from signup → login → dashboard → demo view → share link → client payment needed to work.

## Stack
- Next.js 14, Stripe API (raw fetch), Supabase, Vercel

## Integrations
- **Stripe Checkout (embedded)** — £350 payment sessions via raw API
- **Supabase** — demo_links, lead_assignments, sales_users
- **Vercel** — production deployment

## How to verify
1. Signup at `/signup` → complete wizard → lands on dashboard ✅
2. Login at `/login` with name + PIN → redirects to dashboard ✅
3. Admin assigns demo at `/admin/assign` (password: salesflow2026) ✅
4. SP opens lead → taps "View Demo" → sees demo + "Send to Client" button ✅
5. SP taps "Send to Client" → share panel with Copy/WhatsApp/SMS/Email/QR ✅
6. Client opens shared link → sees demo + "Get this website" (no SP UI) ✅
7. Client taps "Get this website" → wizard → Stripe checkout loads → can pay ✅

## Known issues
- Dashboard row click to lead detail sometimes doesn't fire (router.push in onClick on div — works but click might be captured by child elements)
- Admin panel nav links don't navigate via client-side routing (direct URL works)
- Stripe buttons in the embedded checkout can't be automated (cross-origin iframe) — must test payment completion manually with test card 4242 4242 4242 4242
